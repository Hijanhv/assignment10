{
  "name": "bookmarks-review",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone package to run the bookmarks route tests for the code-review exercise.",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.4.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
